// Instructions for the Notes App Functionality:

// 1. Add a note when the "+ Add Note" button is clicked:
//    - Prompt the user to input the note's content.
//    - Dynamically create a new note element in the notes container.
//    - Add the content to local storage to persist the note.

// 2. Display notes saved in local storage:
//    - When the page loads, retrieve all saved notes from local storage.
//    - Render each note in the notes container.

// 3. Delete a note:
//    - Add a delete button to each note.
//    - When the delete button is clicked, remove the note from both the DOM and local storage.

// Steps to Implement in JavaScript:
//    a) Select the "+ Add Note" button and notes container with "getElementById".
//    b) Create a function to handle creating and displaying new notes.
//    c) Create a function to delete notes both from the DOM and local storage.
//    d) Store and retrieve notes using "localStorage.setItem" and "localStorage.getItem".

// Use the above instructions to implement the required functionality in a separate JS file.
